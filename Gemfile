# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem "newrelic_rpm"

# gem "psych", '~> 3' # => OK
gem "psych", '~> 4' # => ** [NewRelic] FATAL : Failed to read or parse configuration file at config/newrelic.yml
